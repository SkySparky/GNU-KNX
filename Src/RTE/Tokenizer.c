#include "Tokenizer.h"

Token * tokenize(char * raw, Node * node){

  Token * head = NULL;

  char * iter = raw;//string iterator
  char * ldex = raw;//last cut index

  while(iter){



    ++iter;
  }

  return head;
}
