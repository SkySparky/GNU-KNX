Make comprehensive validity check in parser so no syntax errors make it to the
execute function

Truncate beginning of library file path/name when adding to module

Binary trees for hashed lookups (Functions/variables)

Clean up the circular dependencies

Add ark support
