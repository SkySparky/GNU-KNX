#include "State.h"

//register new node
bool validateNode(state*st)
{
printf("validateNode called\n");

return true;
}

//deregister node
bool invalidateNode(state*st)
{
printf("invalidateNode called\n");

return true;
}